import { themeColor } from "../colors/theme";
import { textColor } from "../colors/text";

const formTheme = {
  labelRequiredMarkColor: themeColor.danger,
  labelColor: textColor.default,
};
export default formTheme;
