import { themeColor } from "../colors/theme";
import { textColor } from "../colors/text";
import { bgColor } from "../colors/background";

const inputTheme = {
  hoverBorderColor: textColor.highlight,
  activeBg: bgColor.third,
  activeBorderColor: textColor.highlight,
  activeShadow: "none",
  errorActiveShadow: "none",
  warningActiveShadow: "none",
};
export { inputTheme };
